name: Test GitHub-Hosted Runner

on:
  push:
    branches:
      - "main"

jobs:
  test-github-runner:
    runs-on: [ip-172-31-23-39]

    steps:
      - name: Print environment
        run: |
          echo "User: $(whoami)"
          echo "Shell: $SHELL"
          which bash
          env
