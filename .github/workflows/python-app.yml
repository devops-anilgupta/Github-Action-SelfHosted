name: Test EC2 Runner

on:
  push:
    branches:
      - main

jobs:
  test-ec2:
    runs-on: [self-hosted, linux]  # Match the runner's label exactly

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Python script
        run: python3 my_script.py
