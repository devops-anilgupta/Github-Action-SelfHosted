name: Test GitHub-Hosted Runner

on:
  push:
    branches:
      - "main"

jobs:
  test-github-runner:
    runs-on: [self-hosted, Linux, X64]

    steps:
      - name: Print environment
        run: |
          echo "User: $(whoami)"
          echo "Shell: $SHELL"
          which bash
          env
