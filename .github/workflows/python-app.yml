name: Test Self-Hosted Runner

on:
  push:
    branches:
      - "**"

jobs:
  run-on-ec2:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - run: hostname
      - run: uname -a
      - run: echo "Runner name is ${{ runner.name }}"
      - run: date
